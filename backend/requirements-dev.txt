# Dev / test dependencies. Install with: pip install -r requirements-dev.txt
pytest>=8.0
# httpx 0.28+ broke Starlette's TestClient (`app=` shortcut) — pin
# under 0.28 until upstream resolves.
httpx>=0.27,<0.28
